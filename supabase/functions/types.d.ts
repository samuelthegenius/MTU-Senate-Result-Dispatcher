// Type declarations for Deno Edge Functions
// All types (Deno, Request, Response, fetch, etc.) are provided by the Deno VS Code extension
// and the "DOM" lib in tsconfig.json - no manual declarations needed
